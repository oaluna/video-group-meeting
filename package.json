{
  "scripts": {
    "prepare-client":"cd client && yarn && yarn run build",
    "prepare-server":"cd server && yarn install",
    "heroku-postbuild": "yarn run prepare-client && yarn run prepare-server",
    "start": "cd server && yarn start"
  },
  "homepage": "https://video-group-meeting.herokuapp.com/"
}
